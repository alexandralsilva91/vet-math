/**
 * Central API configuration.
 *
 * During development, the Vite proxy forwards /api/* to localhost:3001,
 * so we use a relative path here. In production, replace this with your
 * real backend URL or an environment variable.
 */
export const API_BASE_URL = '/api';
